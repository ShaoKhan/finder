<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Fundmeldung</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            font-size: 12px;
            margin: 20px;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }

        th {
            background-color: #f2f2f2;
        }

        .image {
            max-width: 300px;
            height: auto;
            display: block;
            margin: 0 auto;
        }
    </style>
</head>
<body>
<h1>Fundmeldung</h1>

<table>
    <tr>
        <th>Name</th>
        <td>{{ image.name }}</td>
    </tr>
    <tr>
        <th>Nächster Ortskern / Kirche</th>
        <td>{{ image.nearestTown }} in {{ image.distanceToChurchOrCenter }} km</td>
    </tr>
    <tr>
        <th>Erstellt am:</th>
        <td>{{ image.createdAt|date('d.m.Y H:i') }}</td>
    </tr>
    <tr>
        <th>Koordinaten</th>
        <td>
            {% if image.latitude and image.longitude %}
                {{ image.utmX }}, {{ image.utmY }}<br />
                ({{ image.latitude }}, {{ image.longitude }})
            {% else %}
                N/A
            {% endif %}
        </td>
    </tr>
    <tr>
        <th>Gemarkung</th>
        <td>{{ image.parcel }}</td>
    </tr>
    <tr>
        <th>District</th>
        <td>{{ image.district }}</td>
    </tr>
</table>

{% if image.filePath %}
    <h2>Bild</h2>
    <img src="{{ absolute_url(asset('fundbilder/' ~ image.filePath)) }}" alt="{{ image.name }}" class="image">
{% endif %}
</body>
</html>
